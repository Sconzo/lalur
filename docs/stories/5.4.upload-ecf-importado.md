# Story 5.4: Upload e Armazenamento do ECF Importado

## Status

**Ready for Review**

---

## Story

**As a** CONTADOR,
**I want** fazer upload do ECF completo gerado por sistema externo (Receita Federal / outro sistema),
**so that** o sistema armazene o ECF Importado e permita posteriormente fazer o merge com o Arquivo Parcial.

---

## Acceptance Criteria

1. Use case `UploadImportedEcfUseCase` criado em `application/port/in/`

2. Service `EcfUploadService` implementa o use case com os seguintes passos:
   - **Passo 1**: Valida extensão do arquivo (deve ser `.txt`)
   - **Passo 2**: Valida tamanho (máximo 50MB)
   - **Passo 3**: Valida formato SPED básico (todas as linhas devem iniciar e terminar com `|`)
   - **Passo 4**: Valida que o arquivo contém bloco M (`|M001|` presente)
   - **Passo 5**: Lê conteúdo do arquivo como String usando encoding **ISO-8859-1 (LATIN-1)** — padrão SPED ECF
   - **Passo 6**: Cria `EcfFile` com `fileType = IMPORTED_ECF`, `fileStatus = DRAFT`, `content = string do arquivo`, `fileName = "ECF_Importado_{fiscalYear}_{cnpj}.txt"`, `generatedAt = now()`, `generatedBy = usuário autenticado`
   - **Passo 7**: Persiste via `EcfFileRepositoryPort.saveOrReplace` (upsert — substitui se já existir)
   - **Passo 8**: Se existir `COMPLETE_ECF` para esta empresa e ano com status `VALIDATED` ou `FINALIZED`, rebaixa para `DRAFT` e limpa `validationErrors`

3. Endpoint: `POST /api/v1/ecf/upload-importado` (autenticado, `multipart/form-data`, requer header `X-Company-Id`)
   - Query param: `fiscalYear` (Integer, obrigatório)
   - Body: arquivo `.txt` como `MultipartFile`

4. DTO `UploadImportedEcfResponse`: `success` (boolean), `message` (String), `ecfFileId` (Long), `fileName` (String), `fileSizeBytes` (Long), `lineCount` (Integer)

5. Se já existir um `IMPORTED_ECF` para a empresa e ano fiscal: upsert (atualiza o registro existente, não cria novo)

6. Nome armazenado: `ECF_Importado_{fiscalYear}_{cnpj}.txt`

7. Response 200 OK com metadados

8. Response 400 Bad Request se:
   - Extensão inválida: `"O arquivo deve ter extensão .txt"`
   - Tamanho excedido: `"O arquivo excede o tamanho máximo de 50MB"`
   - Formato SPED inválido: `"O arquivo não está no formato SPED (linhas devem iniciar e terminar com |)"`
   - Bloco M ausente: `"O arquivo não contém bloco M (|M001| não encontrado)"`

9. Response 403 Forbidden se empresa não pertence ao CONTADOR autenticado

10. Testes de integração:
    - Upload bem-sucedido de ECF válido retorna 200 com metadados corretos
    - Arquivo sem `|M001|` retorna 400
    - `EcfFile.fileType = IMPORTED_ECF` após upload
    - Reimport (mesmo tipo+empresa+ano) substitui o registro anterior (upsert)
    - COMPLETE_ECF existente com VALIDATED é rebaixado para DRAFT após reimport
    - 403 com empresa de outro CONTADOR

---

## Tasks / Subtasks

- [x] **Task 1: Criar Use Case Interface** (AC: 1)
  - [x] Criar `UploadImportedEcfUseCase` em `application/port/in/ecf/`
  - [x] Método: `UploadImportedEcfResponse upload(byte[] fileContent, String originalFileName, Integer fiscalYear, Long companyId, String generatedBy)`

- [x] **Task 2: Criar DTO de Response** (AC: 4)
  - [x] Criar `UploadImportedEcfResponse` em `infrastructure/dto/ecf/`
    - `boolean success`, `String message`, `Long ecfFileId`, `String fileName`, `Long fileSizeBytes`, `Integer lineCount`

- [x] **Task 3: Criar Service** (AC: 2)
  - [x] Criar `EcfUploadService` em `application/service/`
  - [x] Implementar `UploadImportedEcfUseCase`
  - [x] Injetar: `EcfFileRepositoryPort`, `CompanyRepositoryPort`
  - [x] Implementar Passo 1: validar extensão (`.txt`)
  - [x] Implementar Passo 2: validar tamanho (≤ 50MB)
  - [x] Implementar Passo 3: verificar formato `|...|` nas primeiras 50 linhas
  - [x] Implementar Passo 4: verificar presença de `|M001|`
  - [x] Implementar Passo 5: converter `byte[]` para `String` com `ISO_8859_1`
  - [x] Implementar Passo 6: construir `EcfFile` domain object
  - [x] Implementar Passo 7: `saveOrReplace`
  - [x] Implementar Passo 8: rebaixar COMPLETE_ECF para DRAFT se necessário
  - [x] Calcular `lineCount` = número de linhas do arquivo

- [x] **Task 4: Criar Controller REST** (AC: 3, 6, 7, 8, 9)
  - [x] Adicionar endpoint `POST /api/v1/ecf/upload-importado` em `EcfController`
  - [x] Receber `@RequestParam("file") MultipartFile file` e `@RequestParam Integer fiscalYear`
  - [x] Company ID via `CompanyContext.getCurrentCompanyId()`
  - [x] `@PreAuthorize("hasRole('CONTADOR')")`

- [ ] **Task 5: Criar Testes de Integração** (AC: 10)
  - [ ] ⚠️ Requer Docker/TestContainers — ambiente atual não suporta Docker

---

## Dev Notes

### Arquitetura

Fluxo: `EcfController` → `EcfUploadService` (implements use case) → `EcfFileRepositoryPort` (persistência)

### Source Tree

```
src/main/java/br/com/lalurecf/
├── application/
│   ├── port/
│   │   └── in/
│   │       └── UploadImportedEcfUseCase.java
│   └── service/
│       └── EcfUploadService.java
└── infrastructure/
    └── dto/
        └── response/
            └── UploadImportedEcfResponse.java

src/test/java/br/com/lalurecf/
└── infrastructure/adapter/in/rest/
    └── UploadImportedEcfIntegrationTest.java
```

### Encoding LATIN-1 (ISO-8859-1)

O padrão SPED ECF usa LATIN-1. Converter `MultipartFile.getBytes()` para String:

```java
String content = new String(fileBytes, StandardCharsets.ISO_8859_1);
```

**Não usar** `UTF-8` — caracteres especiais do ECF (acentos, etc.) serão corrompidos.

### Validação de formato SPED (eficiente)

Para arquivos grandes, não é necessário validar todas as linhas. Validar as primeiras N linhas e a presença de `|M001|` é suficiente:

```java
boolean isValidSped = Arrays.stream(content.split("\n", 100))
    .limit(50)
    .allMatch(line -> line.trim().startsWith("|") && line.trim().endsWith("|"));
```

### Configuração de upload (multipart)

Verificar `application.yml` e `application-dev.yml` para limite de tamanho de upload Spring:

```yaml
spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 55MB
```

### Cálculo de lineCount

```java
int lineCount = (int) content.lines().count();
```

### Arquivo ECF mock para testes

```
|0000|...|
|0001|...|
...
|M001|0|1|
|M010|...|
|M030|01012024|31012024|A01|
|M300|6|Provisoes|A|3|1000,00|Historico|
|M305|2130306|1000,00|D|
|M990|7|
|9001|0|
|9900|M001|1|
|9900|M010|1|
|9990|...|
|9999|...|
```

### Testing

**MultipartFile em testes:**

```java
MockMultipartFile mockFile = new MockMultipartFile(
    "file",
    "ecf_2024.txt",
    "text/plain",
    ecfContent.getBytes(StandardCharsets.ISO_8859_1)
);
mockMvc.perform(multipart("/api/v1/ecf/upload-importado")
    .file(mockFile)
    .param("fiscalYear", "2024")
    .header("X-Company-Id", companyId)
    .header("Authorization", "Bearer " + token));
```

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-11 | 1.0 | Story criada a partir do Epic 5 | PM Agent |

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Debug Log References
- Linha 109 de EcfUploadService tinha 101 chars (limite 100) — quebrada em 2 linhas

### Completion Notes List
- Lê bytes do arquivo antes de validar formato para evitar `IOException` depois das validações
- Validação de formato SPED usa `.limit(50)` para eficiência em arquivos grandes
- Task 5 (testes de integração) requer Docker/TestContainers — ambiente sem Docker

### File List
- src/main/java/br/com/lalurecf/application/port/in/ecf/UploadImportedEcfUseCase.java (criado)
- src/main/java/br/com/lalurecf/infrastructure/dto/ecf/UploadImportedEcfResponse.java (criado)
- src/main/java/br/com/lalurecf/application/service/EcfUploadService.java (criado)
- src/main/java/br/com/lalurecf/infrastructure/adapter/in/rest/EcfController.java (modificado — endpoint upload-importado adicionado)

---

## QA Results

*A ser preenchido após revisão de QA*
